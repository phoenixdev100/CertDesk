# Base path the frontend uses to reach the backend API.
# In dev, requests go through the Vite proxy - leave this as /api.
VITE_API_BASE=/api

# Where the Vite dev server proxies /api requests to.
API_PROXY_TARGET=http://localhost:3001

# Dev server port
PORT=5173
